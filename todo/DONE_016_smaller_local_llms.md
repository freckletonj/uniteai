# 016 Smaller Local LLMs

This project should work with `transformers` in general, and should come with a smaller LLM as standard. It should be downloaded at runtime (via transformers) and "Just Work".
